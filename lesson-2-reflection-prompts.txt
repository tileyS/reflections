What happens when you initialize a repository? Why do you need to do it?
  you create a repository which later on when commitments are made git will keep track of the history. Basically telling git that it will be working with this repository. 

How is the staging area different from the working directory and the repository?
What value do you think it offers?
The staginig directory allows exacty what changes to be committed. Allows you to add more than 1 file in a commit; i.e these go together..

How can you use the staging area to make sure you have one commit per logical
change?
You can compare the staging area with the last commit. 

What are some situations when branches would be helpful in keeping your history
organized? How would branches help?
When you are trying out ideas but are not ready to change your original idea.

How do the diagrams help you visualize the branch structure?
Helps me to quickly take a look at latest commits, parents of commits and branches. 

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?
The result is a new commit which contains the history from both branches. The merged branch contains the latest version of the developed software.

What are the pros and cons of Git's automatic merging vs. always doing merges
manually?
